{
  "name": "employee-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix fieldpulse-api install && npm --prefix fieldpulse install && npm --prefix fieldpulse run build",
    "start": "npm --prefix fieldpulse-api run start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
