{
  "name": "employee-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix fieldpulse-api && npm install --prefix fieldpulse && npm run build --prefix fieldpulse",
    "start": "npm start --prefix fieldpulse-api"
  }
}
