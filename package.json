{
  "name": "employee-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build-backend && npm run build-frontend",
    "build-backend": "cd fieldpulse-api && npm install",
    "build-frontend": "cd fieldpulse && npm install --include=dev && npm run build",
    "start": "cd fieldpulse-api && npm start"
  }
}
